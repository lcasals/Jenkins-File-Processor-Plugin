<?xml version="1.0" encoding="UTF-8"?>
<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
<div>Description: Document Data Validator is a Jenkins plugin that extracts metadata from PDF, Word, and text files, validates the content,
    and saves metadata into JSON files in the Workspace. It detects and lists files in a directory, prints out file information,
    and reports errors such as broken links to the console.
</div>

</j:jelly>
